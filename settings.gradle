plugins {
    // Allows Gradle to auto-download required JDK toolchains (like Java 22).
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'example-plugin'
